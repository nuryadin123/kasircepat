import { Product, Sale } from '@/types';

// mockUsers is no longer needed as we are using Firebase Auth
// and storing users in Firestore.

export const mockProducts: Product[] = [];

export const mockSales: Sale[] = [];
